AI_GATEWAY_API_KEY=
DEEPSEEK_API_KEY=

# =====================================================================
# 源数据管理 API 配置
# =====================================================================

# 是否使用模拟数据（开发环境可用 true，生产环境请使用 false）
# 当设置为 false 时，需要在 app/api/source-data/route.ts 中配置真实的 API 调用
USE_MOCK_SOURCE_DATA=true

# 真实后端 API 地址（当 USE_MOCK_SOURCE_DATA=false 时使用）
# REAL_API_URL=https://your-api.com/source-data

# API 认证令牌（如果需要）
# API_TOKEN=your-api-token-here
